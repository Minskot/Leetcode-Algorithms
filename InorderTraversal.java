import java.util.ArrayList;
import java.util.List;

/*
        public List<Integer> inorderTra(TreeNode node){
            List<Integer>ans=new ArrayList<>();
            inord(node,ans);
            return ans;
        }

        public void inord(TreeNode node, List<Integer> ans){
        if (node==null) return;
        inord(node.left,ans);
        ans.add(node.val);
        inord(node.right,ans);
        }

*/



public class InorderTraversal {
    /**
     * Definition for a binary tree node.
     */


}
